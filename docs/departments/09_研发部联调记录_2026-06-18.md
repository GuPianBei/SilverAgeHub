# 研发部联调记录：2026-06-18

## 本轮目标

按《08_研发部执行单_稳定架构》将正式调用入口收敛为 `userApi` 和 `adminApi`，
并准备真实云环境联调。

## 本地已完成

- AppID：`wx9f9b638a412469b0`
- envId：`cloud1-d0g5eg4kxeed0d74d`
- 用户端 service 统一调用 `userApi`
- 管理端 service 统一调用 `adminApi`
- 管理页面位于 `miniprogram/admin` 独立分包
- `adminApi` 使用 `admin_user` 集合校验 OpenID
- `adminApi` 不使用 `ADMIN_OPENIDS`
- `userApi`、`adminApi` 版本均为 `2026-06-18-stable-v1`
- 云函数响应统一为 `{ code, message, data }`
- `guide`、`course`、`order` 更新不写入未约定的 `updateTime`
- `order.status` 仅允许 `未确认`、`已确认`、`完成`
- 管理端支持教程和课程增删改查、预约状态更新

## 本地自测

执行：

```powershell
node scripts/verify-stable-architecture.js
```

结果：通过。

已额外模拟微信云上下文验证：

- `userApi` 的 `health` 返回稳定版版本号
- `adminApi` 的 `whoami` 返回 OpenID 和管理员状态
- 未授权管理请求返回 `403`

## 云端当前状态

根据 2026-06-18 云开发控制台截图：

- 环境 ID 正确
- 云函数数量显示为 5
- 数据库集合只有 `guide`、`course`、`order`
- 缺少 `admin_user`
- 尚未取得 `userApi health` 的云端版本回执
- 尚未取得 `adminApi whoami` 的云端稳定版回执

因此真实联调当前停在执行单第 2 步。

## 下一步

1. 在当前环境创建 `admin_user` 集合。
2. 开启微信开发者工具的 CLI 服务端口后执行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/deploy-stable-cloud.ps1 -Port 服务端口
```

该脚本只部署 `userApi` 和 `adminApi`，并使用云端安装依赖。
3. 云端测试 `{ "action": "health" }`，确认版本为
   `2026-06-18-stable-v1`。
4. 确认 `adminApi` 已由同一脚本部署。
5. 云端测试 `{ "action": "whoami" }`，记录 `data.openid`。
6. 在 `admin_user` 新增启用的管理员记录。
7. 再次测试 `whoami`，确认 `data.isAdmin = true`。
8. 继续用户端三页、管理 CRUD 和预约闭环联调。

联调期间不要重新部署旧的 `guide`、`course`、`order`、`admin` 云函数。

## 自动化尝试记录

- 已确认微信开发者工具官方 CLI 支持 `cloud functions deploy`。
- 已生成 `scripts/deploy-stable-cloud.ps1`。
- 当前已打开的开发者工具没有可连接的 CLI 服务端口，命令会等待超时。
- 在开发者工具的设置中开启服务端口后，可继续自动部署。
